version: '3.8'

services:
  load_balancer:
    build:
      context: .
      dockerfile: ./load_balancer/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - FLASK_ENV=development

  server1:
    build:
      context: ./myserver
    ports:
      - "5001:5000"
    environment:
      - SERVER_ID=1

  server2:
    build:
      context: ./myserver
    ports:
      - "5002:5000"
    environment:
      - SERVER_ID=2

  server3:
    build:
      context: ./myserver
    ports:
      - "5003:5000"
    environment:
      - SERVER_ID=3